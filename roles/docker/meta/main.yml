---
# SPDX-License-Identifier: MIT-0
# Role metadata for docker

galaxy_info:
  role_name: docker
  author: Ansible Docker Configuration
  description: Install and configure Docker CE (Community Edition) with containerd, BuildKit, and Docker Compose plugin on Fedora systems.
  company: personal

  license: MIT-0

  min_ansible_version: "2.15"

  platforms:
    - name: Fedora
      versions:
        - all

  galaxy_tags:
    - docker
    - containers
    - containerization
    - dockerce
    - containerd
    - devops

dependencies: []
  # List your role dependencies here, one per line.
  # Consider adding 'common' as a dependency for base system configuration.
  # - common
